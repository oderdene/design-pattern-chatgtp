## 1️⃣ Association (Холбоо)

**Тайлбар:**

- **Association** гэдэг нь хоёр объект хоорондын **нийтлэг холбоо** юм.
- Холбоо нь **харилцан харагдах**, гэхдээ нэг объект устсан ч нөгөө нь үлддэг.
- Энгийн хэлбэр: “has-a” буюу “Байдаг”.

**Жишээ:**

```python
class Teacher:
    def __init__(self, name):
        self.name = name

class Student:
    def __init__(self, name):
        self.name = name
        self.teachers = []  # Association

    def add_teacher(self, teacher):
        self.teachers.append(teacher)

# Үүсгэх
t1 = Teacher("Mr. Smith")
s1 = Student("John")
s1.add_teacher(t1)

print(s1.teachers[0].name)  # Mr. Smith
```

✅ **Ялгаа:** s1 устсан ч t1 устахгүй.

---

## 2️⃣ Dependency (Хамаарал)

**Тайлбар:**

- **Dependency** нь нэг класс нөгөө классын **тусламжтайгаар ажиллаж байгаа** тохиолдол.
- Богино хугацааны холбоо, объект заавал хадгалагдах шаардлагагүй.

**Жишээ:**

```python
class Printer:
    def print_document(self, document):
        print("Printing:", document)

class Computer:
    def use_printer(self, printer, document):  # Dependency
        printer.print_document(document)

# Үүсгэх
printer = Printer()
pc = Computer()
pc.use_printer(printer, "Report.pdf")
```

✅ **Ялгаа:** Computer объектын **use_printer** функц printer объекттой зөвхөн функц дотор харьцаж байна, удаан хугацааны холбоо байхгүй.

---

## 3️⃣ Aggregation (Нэгтгэл)

**Тайлбар:**

- **Aggregation** нь “part-of” харилцаа, **слаб холбоотой**, гэхдээ нэгтгэгдсэн объект устсан ч өөр нь үлдэнэ.
- Үйл ажиллагааны хувьд **“whole-part”** холбоо.

**Жишээ:**

```python
class Department:
    def __init__(self, name):
        self.name = name
        self.employees = []  # Aggregation

    def add_employee(self, employee):
        self.employees.append(employee)

class Employee:
    def __init__(self, name):
        self.name = name

# Үүсгэх
e1 = Employee("Alice")
d1 = Department("IT")
d1.add_employee(e1)

# Department устахад Employee устахгүй
del d1
print(e1.name)  # Alice
```

✅ **Ялгаа:** whole устсан ч part устдаггүй.

---

## 4️⃣ Composition (Бүтэц)

**Тайлбар:**

- **Composition** нь **маш хүчтэй whole-part холбоо**.
- Whole уствал part мөн устна.
- “Бүтэц доторх объект нь зөвхөн whole-д байдаг”.

**Жишээ:**

```python
class House:
    def __init__(self):
        self.rooms = [self.Room("Living Room"), self.Room("Bedroom")]

    class Room:
        def __init__(self, name):
            self.name = name

# Үүсгэх
house = House()
for room in house.rooms:
    print(room.name)

# House устахад Room-ууд бас устна
del house
# print(house.rooms[0].name) -> Error! House устсан
```

```python
class Battery:
    def __init__(self):
        self.charge = 100

    def use(self):
        self.charge -= 10
        print(f"Battery charge is now {self.charge}%")

class Phone:
    def __init__(self, model):
        self.model = model
        self.battery = Battery()  # Composed inside

    def use_phone(self):
        print(f"Using phone {self.model}")
        self.battery.use()

# Create object
phone = Phone("iPhone")
phone.use_phone()
```

✅ **Ялгаа:** part объект зөвхөн whole-д харьяалагддаг, standalone байх боломжгүй.

---

## 🔹 Хураангуй ялгаа

| Холбоо төрөл | Амьдралын хугацаа              | Example             | Whole уствал Part? |
| ------------ | ------------------------------ | ------------------- | ------------------ |
| Association  | Хоёулаа бие даан амьдарч болно | Student–Teacher     | Үгүй               |
| Dependency   | Түр зуурын харилцаа            | Computer–Printer    | Үгүй               |
| Aggregation  | Слаб whole-part                | Department–Employee | Үгүй               |
| Composition  | Бүрэн whole-part               | House–Room          | Тийм               |

---

## 5️⃣ Object үүсгэх ба устгах

- **Үүсгэх:** `obj = ClassName()`
- **Устгах:** `del obj`
- **Composition**-д устгах үед part объект автоматаар устна.
- **Aggregation/Association**-д устгах үед өөр объект үлдэнэ.

---

Sure! Here's how you can represent **Association**, **Aggregation**, **Composition**, and **Dependency** in **Java**, with full code and object creation.

---

## ✅ 1. **Association** Example (Driver ↔ Car)

```java
class Driver {
    String name;

    Driver(String name) {
        this.name = name;
    }

    void drive() {
        System.out.println(name + " is driving.");
    }
}

class Car {
    String model;
    Driver driver; // Associated, but not owned

    Car(String model) {
        this.model = model;
    }

    void assignDriver(Driver driver) {
        this.driver = driver;
        System.out.println(driver.name + " is assigned to car " + model);
    }
}

public class AssociationExample {
    public static void main(String[] args) {
        Driver d = new Driver("Alice");
        Car c = new Car("Toyota");
        c.assignDriver(d);
        d.drive();
    }
}
```

---

## ✅ 2. **Aggregation** Example (Car has Engine, but engine exists independently)

```java
class Engine {
    int horsepower;

    Engine(int horsepower) {
        this.horsepower = horsepower;
    }
}

class Car {
    String model;
    Engine engine; // Aggregated

    Car(String model, Engine engine) {
        this.model = model;
        this.engine = engine;
    }

    void showDetails() {
        System.out.println(model + " has an engine with " + engine.horsepower + " HP.");
    }
}

public class AggregationExample {
    public static void main(String[] args) {
        Engine e = new Engine(200);
        Car c = new Car("BMW", e);
        c.showDetails();
    }
}
```

---

## ✅ 3. **Composition** Example (Phone owns Battery)

```java
class Battery {
    int charge = 100;

    void use() {
        charge -= 10;
        System.out.println("Battery charge: " + charge + "%");
    }
}

class Phone {
    String model;
    Battery battery; // Composed (created inside)

    Phone(String model) {
        this.model = model;
        this.battery = new Battery(); // Composition
    }

    void usePhone() {
        System.out.println("Using phone " + model);
        battery.use();
    }
}

public class CompositionExample {
    public static void main(String[] args) {
        Phone p = new Phone("iPhone");
        p.usePhone();
    }
}
```

---

## ✅ 4. **Dependency** Example (DocumentEditor uses Printer temporarily)

```java
class Printer {
    void print(String text) {
        System.out.println("Printing: " + text);
    }
}

class DocumentEditor {
    void editAndPrint(String content) {
        Printer printer = new Printer(); // Temporary use = Dependency
        printer.print(content);
    }
}

public class DependencyExample {
    public static void main(String[] args) {
        DocumentEditor editor = new DocumentEditor();
        editor.editAndPrint("Dependency example in Java");
    }
}
```

---

## 🧠 Summary

| Type        | Object Created Inside | Lifetime Bound | Example             |
| ----------- | --------------------- | -------------- | ------------------- |
| Association | No                    | No             | Driver ↔ Car        |
| Aggregation | No                    | No             | Car has Engine      |
| Composition | Yes                   | Yes            | Phone owns Battery  |
| Dependency  | Temporary use only    | No             | Editor uses Printer |
